export enum UserStatusEnum {
  ACTIVE = "active",
  INACTIVE = "inactive",
}
export enum UserTypeEnum {
  GUEST = "guest",
  HOST = "host",
}

export enum UserActionEnum {
  ACTIVATE = "activate",
  DEACTIVATE = "deactivate",
}
